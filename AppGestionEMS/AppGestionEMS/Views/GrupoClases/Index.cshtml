@model IEnumerable<AppGestionEMS.Models.GrupoClases>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.asiGdocente.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.aula)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.maxAlum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.minAlum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.asiGdocente.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.aula)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.maxAlum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.minAlum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
